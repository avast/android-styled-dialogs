buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:1.0.0'
    }
}

allprojects {
    repositories {
        jcenter()
    }
}

ext {
    VERSION_NAME = "2.1.0"
    VERSION_CODE = 7
}

apply plugin: 'android-reporting'